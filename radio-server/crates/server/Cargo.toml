[package]
name = "server"
version = "0.1.0"
edition = "2021"

[dependencies]
capture = { path = "../capture" }
encoder = { path = "../encoder" }
tokio = { version = "1.0", features = ["full"] }
tokio-util = "0.7"
bytes = "1.0"
